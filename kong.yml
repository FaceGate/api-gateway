_format_version: "1.1"

services:
  - name: rekognizer
    url: http://rekognizer-dev:8000
    routes:
      - name: verify
        paths:
          - /
      - name: identify
        paths:
          - /
